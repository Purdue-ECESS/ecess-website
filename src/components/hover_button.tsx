import {Button, withStyles} from "@material-ui/core";

export const HoverButtons = withStyles({
    root: {
        color: "#CEB888",
        animation: "linear",
        variant: "contained"
    },
})(Button)